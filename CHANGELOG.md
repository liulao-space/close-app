# 更新日志

版本号取自 `src/Info.plist` 的 `CFBundleShortVersionString`，对应的 git tag 同名（如 `v1.1`）。日期是打 tag 那天。

## v1.1.1 — 2026-09-16

### 新增：自动检查更新

装上这个版本之后，以后再有更新就不用你自己去翻 GitHub 了。

- 启动后静默查一次 GitHub 上公开的 release 接口，之后每 6 小时一次
- 有新版本时，展开的面板顶部出现一行「新版本 x.y.z 可用 · 点此查看」，点击打开 Releases 页面
- 菜单栏图标右键里有 **检查更新…**（手动查一次，给结果回话）和 **自动检查更新**（开关，默认开）
- 没有新版、网络不通、请求超时 —— 一律静默，不弹窗、不打扰

### 说明：这件事不需要上架

「通知用户有新版本」常被误以为要上架 + 开发者账号 + 用户授权 —— 那是**系统推送通知**。这一版走的是另一条路：软件自己发一个 HTTPS 请求，读公开的 release 信息。

| | 系统推送通知 | 软件内检查更新（本版本） |
|---|---|---|
| 上架 App Store | 需要 | 不需要 |
| Apple 开发者账号 | 需要 | 不需要 |
| 用户授权「允许通知」 | 需要 | 不需要 |
| 自己的服务器 | 需要 | 不需要（GitHub 免费） |

只发一个只读 GET，不带任何本机信息（请求头里的 `User-Agent: CloseApps/<版本>` 是 GitHub 要求每个请求都带的）。

### 修

- 版本号比较改成按 `.` 分段逐位比数字，而不是字符串比较 —— 否则 `1.10` 会被判成小于 `1.9`，等真发 1.10 那天才发现就晚了
- 内置自测加上更新检查的回归（6 个版本比较用例 + JSON 解析 + 横幅显示/收起），**但自测不联网**：网络那步只做手工验证，免得某天网不好就红一片、分不清是代码坏了还是网坏了

---

## v1.1 — 2026-09-16

**从「一个面板」变成「一个灵动岛」。** 打开方式从一处扩成三处，折叠态做到彻底隐形。
`src/main.swift` 从 702 行涨到 2983 行。

### 新增：灵动岛

- 常驻屏幕顶部正中。鼠标移到**顶部正中**自动展开，移开约 0.45 秒自动收起 —— 停在原地不动、或只是从刘海往下挪，都不会被误收
- **折叠时有三种长相**，按屏幕和开关自动选：
  - 有刘海 + 「折叠时藏进刘海」开着 → 面板**正好铺满刘海那块物理盲区**（摄像头模组挡着，那块屏幕本来就不显示内容），屏幕上看不到任何东西。阴影一并关掉，否则会投到刘海下面露馅。刘海尺寸从系统读，不写死
  - 没有刘海的屏（外接显示器）→ 折成一枚**「假刘海」**：高度 = 菜单栏高度，顶边贴着屏幕最上沿，相当于白送一块刘海
  - 有刘海、但关掉了那个开关 → 一枚胶囊挂在刘海下沿
- **折叠形状三态统一**：上缘两角直角、下缘两角 16pt 圆角（一个式子：`border-radius: 0 0 16px 16px`）。假刘海和胶囊是同一条曲线，展开时从它的下沿无缝长出来
- 展开的面板顶边固定在刘海/菜单栏下沿，只往下长（596×620）
- 双屏时胶囊跟着鼠标走 —— 鼠标跨屏了就挪过去，不用去另一块屏找它

### 新增：三个打开入口

| 入口 | 操作 |
| --- | --- |
| **灵动岛** | 鼠标移到屏幕顶部正中自动展开，移开自动收起 |
| **全局快捷键** | 默认 `⌥⌘K`，随按随开/关，可在面板里改成任意组合（走 Carbon 公开 API，不需要任何系统授权） |
| **菜单栏图标** | 左键开关面板，右键出菜单（灵动岛开关 / 开机启动 / 改快捷键 / 退出） |

### 面板

- **没有 Dock 图标了**（`LSUIElement`）：常驻后台，不占 ⌘Tab。关面板不等于退出应用 —— 退出走菜单栏图标右键
- **能拖，但位置不留痕**：展开后按住标题栏空白处可以挪开面板（**只有展开态能拖**）；**位置不缓存，每次打开都回到初始位置**。标题栏上的按钮（录制快捷键 / 钉住 / 刷新 / 收起）不在拖动区里，点按钮就是点按钮
- **展开/收起是弹簧动画**（约 0.3s，末端带极轻的过冲）：窗口大小、四角圆角、内容透明度挂在**同一条**曲线上一起走。从刘海/假刘海里"长"出来的时候形状连续变化、内容全程淡入，不是"面板先长完、内容再啪地补上"
- **滚动条比系统默认细一半**（17pt → 8pt）。应用多到一屏装不下时才出现
- 悬停的判定范围 = 面板本身 + 折叠命中区（刘海那一带），鼠标停在刘海最上面也不会误收
- 收起后不会立刻自我重开：要等鼠标离开过命中区（或满 1.5 秒兜底）才允许再展开

### 新增：开机自动启动

- macOS 13 及以上用系统的 `SMAppService`，在「系统设置 › 通用 › 登录项与扩展」里能看到、能关
- macOS 11/12 退回 `~/Library/LaunchAgents/com.liulao.closeapps.launch.plist`
- **把 App 挪到别的位置后要重新勾一次**（系统按路径记登录项）

### 新增：调试与自测（普通使用完全不生效）

- `CLOSEAPPS_DEBUG=1` 打状态日志；`CLOSEAPPS_SELFTEST=1` 跑内置时序自测（注入假光标坐标，覆盖悬停、拖动、动画轨迹、折叠形状、滚动条宽度）——**自测只读取系统设置，不会改动你的开机项**
- stdin 指令控制台：手工注入光标、模拟拖动、导出遮罩
- `tools/preview_fake_notch.swift`：不用跑到外接屏跟前、也不需要截屏权限，就能看到折叠态形状。做法是把 app 自己导出的真实遮罩合成成图，**不重画形状**，所以预览和真机永远是同一个形状

### 文档

- README 标题补上「灵动岛版」，并按实际行为重写了一遍交互说明（打开方式、折叠形态、拖动、滚动条、调试）

## v1.0 — 2026-09-12

首个版本。半透明毛玻璃面板，以卡片网格列出正在运行的应用，每张卡片下方跟随显示该应用打开的窗口（含标题）；卡片右上角 **✕** 把应用连同所有辅助进程一起**彻底关闭**（微信的小程序/Helper 进程、Chrome 的 GPU/渲染进程、VS Code 的扩展宿主等一并清理）。

- 点击卡片本身激活该应用（等同点 Dock 图标，在其他桌面也会自动切换过去）
- 点窗口卡打开（前置）那个具体窗口，跨桌面自动切换、最小化自动还原；点窗口卡上的 ✕ 只关闭这一个窗口
- 需要「辅助功能」权限才能读到窗口标题
- 通用二进制（arm64 + x86_64），附使用者安装指南
